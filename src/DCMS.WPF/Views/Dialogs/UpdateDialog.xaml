<Window x:Class="DCMS.WPF.Views.Dialogs.UpdateDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="تحديث متاح" Height="400" Width="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        FlowDirection="RightToLeft"
        ContentRendered="Window_ContentRendered"
        Background="#F5F7FA">
    
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Icon & Title -->
        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,20">
            <TextBlock Text="🔄" FontSize="48" HorizontalAlignment="Center"/>
            <TextBlock Text="تحديث جديد متاح!" FontSize="24" FontWeight="Bold" 
                       Foreground="#2C3E50" HorizontalAlignment="Center" Margin="0,10,0,0"/>
        </StackPanel>

        <!-- Version Info -->
        <Border Grid.Row="1" Background="White" CornerRadius="10" Padding="20" Margin="0,0,0,15">
            <Border.Effect>
                <DropShadowEffect Color="#E0E0E0" BlurRadius="10" ShadowDepth="2" Opacity="0.3"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" HorizontalAlignment="Center">
                    <TextBlock Text="الإصدار الحالي" Foreground="#7F8C8D" FontSize="12"/>
                    <TextBlock x:Name="txtCurrentVersion" Text="1.0.0" FontSize="20" 
                               FontWeight="Bold" Foreground="#E74C3C"/>
                </StackPanel>

                <TextBlock Grid.Column="1" Text="➡️" FontSize="24" VerticalAlignment="Center" Margin="20,0"/>

                <StackPanel Grid.Column="2" HorizontalAlignment="Center">
                    <TextBlock Text="الإصدار الجديد" Foreground="#7F8C8D" FontSize="12"/>
                    <TextBlock x:Name="txtNewVersion" Text="1.0.1" FontSize="20" 
                               FontWeight="Bold" Foreground="#27AE60"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Release Notes -->
        <Border Grid.Row="2" Background="White" CornerRadius="10" Padding="15" Margin="0,0,0,15">
            <Border.Effect>
                <DropShadowEffect Color="#E0E0E0" BlurRadius="10" ShadowDepth="2" Opacity="0.3"/>
            </Border.Effect>
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock Text="ملاحظات الإصدار:" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,10"/>
                    <TextBlock x:Name="txtReleaseNotes" TextWrapping="Wrap" Foreground="#34495E"/>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- Progress Bar -->
        <StackPanel Grid.Row="3" x:Name="pnlProgress" Visibility="Hidden" Margin="0,0,0,15">
            <TextBlock x:Name="txtProgress" Text="جاري التحميل..." 
                       HorizontalAlignment="Center" Foreground="#34495E" Margin="0,0,0,5"/>
            <ProgressBar x:Name="progressBar" Height="20" Minimum="0" Maximum="100" 
                         Foreground="#3498DB" Background="#ECF0F1"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button x:Name="btnUpdate" Content="📥  تحديث الآن" 
                    Width="150" Height="45" FontSize="14" FontWeight="Bold"
                    Background="#3498DB" Foreground="White" 
                    BorderThickness="0" Cursor="Hand"
                    Click="BtnUpdate_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
            
            <Button x:Name="btnExit" Content="❌  إغلاق التطبيق" 
                    Width="150" Height="45" FontSize="14" FontWeight="Bold"
                    Background="#E74C3C" Foreground="White" 
                    BorderThickness="0" Cursor="Hand" Margin="15,0,0,0"
                    Click="BtnExit_Click">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              TextElement.Foreground="{TemplateBinding Foreground}"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>

        <!-- Warning Message -->
        <TextBlock Grid.Row="4" Text="⚠️ يجب التحديث للاستمرار في استخدام التطبيق"
                   Foreground="#E67E22" FontSize="11" FontWeight="Bold"
                   HorizontalAlignment="Center" VerticalAlignment="Bottom" Margin="0,50,0,0"/>
    </Grid>
</Window>
