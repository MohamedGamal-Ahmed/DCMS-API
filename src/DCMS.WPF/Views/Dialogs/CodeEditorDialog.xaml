<Window x:Class="DCMS.WPF.Views.Dialogs.CodeEditorDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding Title}" Height="350" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        FlowDirection="RightToLeft">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="{Binding Title}" FontSize="20" FontWeight="Bold" Margin="0,0,0,20" Grid.Row="0"/>

        <!-- Code -->
        <StackPanel Grid.Row="1" Margin="0,0,0,15">
            <TextBlock Text="الكود *" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Code, UpdateSourceTrigger=PropertyChanged}" Height="30" Padding="5"/>
        </StackPanel>

        <!-- Entity -->
        <StackPanel Grid.Row="2" Margin="0,0,0,15">
            <TextBlock Text="الجهة *" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Entity, UpdateSourceTrigger=PropertyChanged}" Height="30" Padding="5"/>
        </StackPanel>

        <!-- Engineer -->
        <StackPanel Grid.Row="3" Margin="0,0,0,15">
            <TextBlock Text="المهندس" FontWeight="Bold" Margin="0,0,0,5"/>
            <TextBox Text="{Binding Engineer, UpdateSourceTrigger=PropertyChanged}" Height="30" Padding="5"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
            <Button Content="✅ حفظ" 
                    IsDefault="True" 
                    Click="Save_Click"
                    Width="100" Height="35"
                    Background="#4CAF50" Foreground="White"
                    Margin="0,0,10,0"/>
            
            <Button Content="إلغاء" 
                    IsCancel="True"
                    Width="100" Height="35"/>
        </StackPanel>
    </Grid>
</Window>
