<UserControl x:Class="DCMS.WPF.Views.InboundView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             FlowDirection="RightToLeft"
             d:DesignHeight="700" d:DesignWidth="900">

    <Grid Background="#F5F5F5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="White" Padding="30,20" 
                BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <StackPanel>
                <TextBlock Text="إضافة مراسلة واردة" 
                           FontSize="28" 
                           FontWeight="Bold" 
                           Foreground="#333"/>
                <TextBlock Text="أدخل تفاصيل المراسلة الواردة الجديدة" 
                           FontSize="14" 
                           Foreground="#757575"
                           Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Form -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <Border Background="White" 
                    Margin="30" 
                    Padding="30" 
                    CornerRadius="10">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="10" ShadowDepth="0" Opacity="0.1"/>
                </Border.Effect>

                <StackPanel MaxWidth="700">
                    
                    <!-- Row 1: Subject Number (Read-only) -->
                    <TextBlock Text="رقم الموضوع (تلقائي)" 
                               FontSize="14" 
                               FontWeight="Bold" 
                               Foreground="#333"
                               Margin="0,0,0,5"/>
                    <TextBox Text="{Binding SubjectNumber, Mode=OneWay}"
                             Height="40"
                             Padding="10"
                             FontSize="14"
                             IsReadOnly="True"
                             Background="#F0F0F0"
                             Margin="0,0,0,20"/>

                    <!-- Row 2: Code -->
                    <TextBlock Text="كود" 
                               FontSize="14" 
                               FontWeight="Bold" 
                               Foreground="#333"
                               Margin="0,0,0,5"/>
                    <TextBox Text="{Binding Code, UpdateSourceTrigger=PropertyChanged}"
                             Height="40"
                             Padding="10"
                             FontSize="14"
                             Margin="0,0,0,20"/>

                    <!-- Row 3: From Entity -->
                    <TextBlock Text="وارد من جهة" 
                               FontSize="14" 
                               FontWeight="Bold" 
                               Foreground="#333"
                               Margin="0,0,0,5"/>
                    <TextBox Text="{Binding FromEntity, UpdateSourceTrigger=PropertyChanged}"
                             Height="40"
                             Padding="10"
                             FontSize="14"
                             Margin="0,0,0,20"/>

                    <!-- Row 4: From Engineer -->
                    <TextBlock Text="وارد من مهندس/م" 
                               FontSize="14" 
                               FontWeight="Bold" 
                               Foreground="#333"
                               Margin="0,0,0,5"/>
                    <TextBox Text="{Binding FromEngineer, UpdateSourceTrigger=PropertyChanged}"
                             Height="40"
                             Padding="10"
                             FontSize="14"
                             Margin="0,0,0,20"/>

                    <!-- Row 5: Subject -->
                    <TextBlock Text="الموضوع *" 
                               FontSize="14" 
                               FontWeight="Bold" 
                               Foreground="#333"
                               Margin="0,0,0,5"/>
                    <TextBox Text="{Binding Subject, UpdateSourceTrigger=PropertyChanged}"
                             Height="40"
                             Padding="10"
                             FontSize="14"
                             Margin="0,0,0,20"/>

                    <!-- Row 6: Responsible Engineer -->
                    <TextBlock Text="المهندس المسئول" 
                               FontSize="14" 
                               FontWeight="Bold" 
                               Foreground="#333"
                               Margin="0,0,0,5"/>
                    <TextBox Text="{Binding ResponsibleEngineer, UpdateSourceTrigger=PropertyChanged}"
                             Height="40"
                             Padding="10"
                             FontSize="14"
                             Margin="0,0,0,20"/>

                    <!-- Row 7: Attachment URL -->
                    <TextBlock Text="رابط المرفقات" 
                               FontSize="14" 
                               FontWeight="Bold" 
                               Foreground="#333"
                               Margin="0,0,0,5"/>
                    <TextBox Text="{Binding AttachmentUrl, UpdateSourceTrigger=PropertyChanged}"
                             Height="40"
                             Padding="10"
                             FontSize="14"
                             Margin="0,0,0,20"/>

                    <!-- Row 8: Inbound Date -->
                    <TextBlock Text="وارد بتاريخ *" 
                               FontSize="14" 
                               FontWeight="Bold" 
                               Foreground="#333"
                               Margin="0,0,0,5"/>
                    <DatePicker SelectedDate="{Binding InboundDate}"
                                Height="40"
                                FontSize="14"
                                Margin="0,0,0,20"/>

                    <!-- Row 9: Reply / Notes -->
                    <TextBlock Text="الرد / ملاحظات" 
                               FontSize="14" 
                               FontWeight="Bold" 
                               Foreground="#333"
                               Margin="0,0,0,5"/>
                    <TextBox Text="{Binding Reply, UpdateSourceTrigger=PropertyChanged}"
                             Height="100"
                             Padding="10"
                             FontSize="14"
                             TextWrapping="Wrap"
                             AcceptsReturn="True"
                             VerticalScrollBarVisibility="Auto"
                             Margin="0,0,0,30"/>

                    <!-- Action Buttons -->
                    <StackPanel Orientation="Horizontal" 
                                HorizontalAlignment="Left"
                                Margin="0,10,0,0">
                        <Button Content="💾 حفظ"
                                Command="{Binding SaveCommand}"
                                Width="120"
                                Height="45"
                                Background="#4CAF50"
                                Foreground="White"
                                BorderThickness="0"
                                FontSize="16"
                                FontWeight="Bold"
                                Cursor="Hand"
                                Margin="0,0,10,0">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}"
                                                        CornerRadius="5"
                                                        Padding="15,10">
                                                    <ContentPresenter HorizontalAlignment="Center" 
                                                                    VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#45A049"/>
                                        </Trigger>
                                        <Trigger Property="IsEnabled" Value="False">
                                            <Setter Property="Background" Value="#CCC"/>
                                            <Setter Property="Cursor" Value="No"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>

                        <Button Content="🔄 مسح"
                                Command="{Binding ClearCommand}"
                                Width="120"
                                Height="45"
                                Background="#757575"
                                Foreground="White"
                                BorderThickness="0"
                                FontSize="16"
                                FontWeight="Bold"
                                Cursor="Hand">
                            <Button.Style>
                                <Style TargetType="Button">
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="Button">
                                                <Border Background="{TemplateBinding Background}"
                                                        CornerRadius="5"
                                                        Padding="15,10">
                                                    <ContentPresenter HorizontalAlignment="Center" 
                                                                    VerticalAlignment="Center"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#616161"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Button.Style>
                        </Button>
                    </StackPanel>

                    <!-- Loading Indicator -->
                    <ProgressBar IsIndeterminate="True"
                                 Height="3"
                                 Margin="0,20,0,0"
                                 Visibility="{Binding IsSaving, Converter={StaticResource BoolToVisConverter}}"/>
                </StackPanel>
            </Border>
        </ScrollViewer>
    </Grid>
</UserControl>
