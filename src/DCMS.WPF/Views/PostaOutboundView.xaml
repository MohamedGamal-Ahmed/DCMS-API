<UserControl x:Class="DCMS.WPF.Views.PostaOutboundView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:controls="clr-namespace:DCMS.WPF.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1000"
             FlowDirection="RightToLeft">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header -->
            <TextBlock Text="بوسطة الصادر" 
                       Grid.Row="0"
                       FontSize="24" 
                       FontWeight="Bold" 
                       Foreground="#333"
                       Margin="0,0,0,20"/>

            <!-- Form -->
            <Border Grid.Row="1" 
                    Background="White" 
                    CornerRadius="10" 
                    Padding="20"
                    Margin="0,0,0,20">
                <Border.Effect>
                    <DropShadowEffect Color="#DDD" BlurRadius="10" ShadowDepth="2"/>
                </Border.Effect>

                <StackPanel MaxWidth="700">
                    
                    <!-- Auto Number (Read-Only) -->
                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="0,0,15,0">
                            <TextBlock Text="رقم الصادر *" 
                                       FontSize="14" 
                                       FontWeight="Bold" 
                                       Foreground="#333"
                                       Margin="0,0,10,0"/>
                            <TextBlock Text="{Binding SubjectNumber}" 
                                       FontSize="18" 
                                       FontWeight="Bold" 
                                       Foreground="#FF5722"
                                       Background="#FBE9E7"
                                       Padding="10,5"
                                       VerticalAlignment="Center"/>
                        </StackPanel>
                    </Grid>

                    <!-- Code -->
                    <TextBlock Text="كود" 
                               FontSize="14" 
                               FontWeight="Bold" 
                               Foreground="#333"
                               Margin="0,0,0,5"/>
                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <ComboBox Grid.Column="0"
                                  ItemsSource="{Binding AvailableCodes}"
                                  SelectedItem="{Binding SelectedCodeEntry}"
                                  Text="{Binding Code, UpdateSourceTrigger=PropertyChanged}"
                                  IsEditable="True"
                                  DisplayMemberPath="Code"
                                  SelectedValuePath="Code"
                                  Height="40"
                                  Padding="10"
                                  FontSize="14"/>

                        <Button Grid.Column="1"
                                Command="{Binding ManageCodesCommand}"
                                Content="⚙️"
                                ToolTip="إدارة الأكواد"
                                Width="40"
                                Height="40"
                                Margin="5,0,0,0"
                                Background="#E0E0E0"
                                BorderBrush="#BDBDBD"
                                Visibility="{Binding IsAdmin, Converter={StaticResource BooleanToVisibilityConverter}}">
                             <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="5"/>
                                </Style>
                            </Button.Resources>
                        </Button>
                    </Grid>

                    <!-- To Entity -->
                    <TextBlock Text="صادر إلى جهة" 
                               FontSize="14" 
                               FontWeight="Bold" 
                               Foreground="#333"
                               Margin="0,0,0,5"/>
                    <TextBox Text="{Binding ToEntity, UpdateSourceTrigger=PropertyChanged}"
                             Height="40"
                             Padding="10"
                             FontSize="14"
                             IsReadOnly="True"
                             Background="#F5F5F5"
                             Margin="0,0,0,20"/>

                    <!-- To Engineer -->
                    <TextBlock Text="مهندس/ة" 
                               FontSize="14" 
                               FontWeight="Bold" 
                               Foreground="#333"
                               Margin="0,0,0,5"/>
                    <TextBox Text="{Binding ToEngineer, UpdateSourceTrigger=PropertyChanged}"
                             Height="40"
                             Padding="10"
                             FontSize="14"
                             IsReadOnly="True"
                             Background="#F5F5F5"
                             Margin="0,0,0,20"/>

                    <!-- Subject -->
                    <TextBlock Text="الموضوع *" 
                               FontSize="14" 
                               FontWeight="Bold" 
                               Foreground="#333"
                               Margin="0,0,0,5"/>
                    <TextBox Text="{Binding Subject, UpdateSourceTrigger=PropertyChanged}"
                             Height="40"
                             Padding="10"
                             FontSize="14"
                             Margin="0,0,0,20"/>

                    <!-- Related Inbound No -->
                    <TextBlock Text="رقم الوارد المرتبط (اختياري)" 
                               FontSize="14" 
                               FontWeight="Bold" 
                               Foreground="#333"
                               Margin="0,0,0,5"/>
                    <TextBox Text="{Binding RelatedInboundNo, UpdateSourceTrigger=PropertyChanged}"
                             Height="40"
                             Padding="10"
                             FontSize="14"
                             Margin="0,0,0,20"
                             ToolTip="أدخل رقم الوارد المرتبط بهذا الصادر إن وجد"/>

                    <!-- Responsible Engineer -->
                    <TextBlock Text="المهندس المسئول" 
                               FontSize="14" 
                               FontWeight="Bold" 
                               Foreground="#333"
                               Margin="0,0,0,5"/>
                    <controls:EngineerSelectorControl DataContext="{Binding ResponsibleEngineerSelector}" Margin="0,0,0,20"/>

                    <!-- Transferred To -->
                    <TextBlock Text="تحويل إلى" 
                               FontSize="14" 
                               FontWeight="Bold" 
                               Foreground="#333"
                               Margin="0,0,0,5"/>
                    <controls:EngineerSelectorControl DataContext="{Binding TransferredToSelector}" Margin="0,0,0,20"/>

                    <!-- Outbound Date -->
                    <TextBlock Text="صادر بتاريخ *" 
                               FontSize="14" 
                               FontWeight="Bold" 
                               Foreground="#333"
                               Margin="0,0,0,5"/>
                    <DatePicker SelectedDate="{Binding OutboundDate}"
                                Height="40"
                                FontSize="14"
                                Margin="0,0,0,20"/>

                </StackPanel>
            </Border>

            <!-- Action Buttons -->
            <StackPanel Grid.Row="3" 
                        Orientation="Horizontal" 
                        HorizontalAlignment="Right"
                        Margin="0,20,0,0">
                
                <Button Command="{Binding SaveCommand}"
                        Content="💾 حفظ"
                        Width="120"
                        Height="40"
                        Background="#4CAF50"
                        Foreground="White"
                        FontSize="16"
                        FontWeight="Bold"
                        Margin="10,0,0,0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>

                <Button Command="{Binding ClearCommand}"
                        Content="🔄 مسح"
                        Width="120"
                        Height="40"
                        Background="#757575"
                        Foreground="White"
                        FontSize="16"
                        FontWeight="Bold">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>

            </StackPanel>
        </Grid>
    </ScrollViewer>
</UserControl>
