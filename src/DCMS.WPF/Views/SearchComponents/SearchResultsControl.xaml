<UserControl x:Class="DCMS.WPF.Views.SearchComponents.SearchResultsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:enums="clr-namespace:DCMS.Domain.Enums;assembly=DCMS.Domain"
             xmlns:components="clr-namespace:DCMS.WPF.Views.Components"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <!-- Inbound DataGrid -->
        <DataGrid ItemsSource="{Binding SearchResults}" 
                  SelectedItem="{Binding SelectedRecord}"
                  AutoGenerateColumns="False" 
                  IsReadOnly="True"
                  HeadersVisibility="Column" 
                  GridLinesVisibility="All"
                  Background="White" 
                  BorderThickness="0"
                  FontSize="13"
                  AlternatingRowBackground="#F9F9F9">
            <DataGrid.Style>
                <Style TargetType="DataGrid">
                    <Setter Property="Visibility" Value="Visible"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding SelectedRecordType}" Value="{x:Static enums:SearchRecordType.Outbound}">
                            <Setter Property="Visibility" Value="Collapsed"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.Style>
            
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="MinHeight" Value="60"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Status}" Value="Closed">
                            <Setter Property="Background" Value="#E8E8E8"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>
            
            <DataGrid.InputBindings>
                <MouseBinding MouseAction="LeftDoubleClick" 
                              Command="{Binding ViewDetailsCommand}" 
                              CommandParameter="{Binding SelectedItem, RelativeSource={RelativeSource AncestorType=DataGrid}}"/>
            </DataGrid.InputBindings>
            
            <DataGrid.Columns>
                <DataGridTextColumn Header="رقم الموضوع" Width="140" Binding="{Binding SubjectNumber}">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="TextAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="الكود" Width="120" Binding="{Binding Code}">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="TextAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="الموضوع" Width="2*" Binding="{Binding Subject}">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextWrapping" Value="Wrap"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="Padding" Value="8,4"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="المهندس المسئول" Width="150" Binding="{Binding ResponsibleEngineer}">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="TextAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="محول إلى" Width="150" Binding="{Binding TransferredTo}">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="TextAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="تاريخ التحويل" Width="120" Binding="{Binding TransferDate, StringFormat='dd/MM/yyyy'}">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="TextAlignment" Value="Center"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="الرد" Width="2*" Binding="{Binding Reply}">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextWrapping" Value="Wrap"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="Padding" Value="8,4"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTemplateColumn Header="الحالة" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Border CornerRadius="12" Padding="10,4" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#E0E0E0"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Status}" Value="New"><Setter Property="Background" Value="#FFF9C4"/></DataTrigger>
                                            <DataTrigger Binding="{Binding Status}" Value="InProgress"><Setter Property="Background" Value="#BBDEFB"/></DataTrigger>
                                            <DataTrigger Binding="{Binding Status}" Value="Completed"><Setter Property="Background" Value="#C8E6C9"/></DataTrigger>
                                            <DataTrigger Binding="{Binding Status}" Value="Closed"><Setter Property="Background" Value="#CFD8DC"/></DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock Text="{Binding Status}" HorizontalAlignment="Center" FontWeight="Medium" FontSize="12"/>
                            </Border>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#2196F3"/><Setter Property="Foreground" Value="White"/>
                    <Setter Property="FontWeight" Value="Bold"/><Setter Property="FontSize" Value="14"/>
                    <Setter Property="Padding" Value="12,10"/><Setter Property="HorizontalContentAlignment" Value="Center"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>
        </DataGrid>

        <!-- Outbound DataGrid -->
        <DataGrid ItemsSource="{Binding SearchResults}" 
                  SelectedItem="{Binding SelectedRecord}"
                  AutoGenerateColumns="False" 
                  IsReadOnly="True"
                  HeadersVisibility="Column" 
                  GridLinesVisibility="Horizontal"
                  Background="White" 
                  BorderThickness="0"
                  RowHeight="45"
                  FontSize="13"
                  AlternatingRowBackground="#F9F9F9">
            <DataGrid.Style>
                <Style TargetType="DataGrid">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding SelectedRecordType}" Value="{x:Static enums:SearchRecordType.Outbound}">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.Style>
            <DataGrid.InputBindings>
                <MouseBinding MouseAction="LeftDoubleClick" Command="{Binding ViewDetailsCommand}" CommandParameter="{Binding SelectedRecord}"/>
            </DataGrid.InputBindings>
            <DataGrid.Columns>
                <DataGridTextColumn Header="رقم الموضوع" Width="120" Binding="{Binding SubjectNumber}"/>
                <DataGridTextColumn Header="الكود" Width="100" Binding="{Binding Code}"/>
                <DataGridTextColumn Header="المهندس المسؤول" Width="150" Binding="{Binding ResponsibleEngineer}"/>
                <DataGridTextColumn Header="الجهة المرسل إليها" Width="150" Binding="{Binding ToEntity}"/>
                <DataGridTextColumn Header="المهندس المرسل إليه" Width="150" Binding="{Binding ToEngineer}"/>
                <DataGridTextColumn Header="الموضوع" Width="*" Binding="{Binding Subject}"/>
                <DataGridTextColumn Header="التاريخ" Width="110" Binding="{Binding OutboundDate, StringFormat='dd/MM/yyyy'}"/>
                <DataGridTemplateColumn Header="الإجراءات" Width="200">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Command="{Binding DataContext.ViewDetailsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                                    CommandParameter="{Binding}" Content="🔍 تفاصيل" Background="#2196F3" Foreground="White" Padding="12,6" BorderThickness="0" Cursor="Hand">
                                <Button.Resources><Style TargetType="Border"><Setter Property="CornerRadius" Value="4"/></Style></Button.Resources>
                            </Button>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#4CAF50"/><Setter Property="Foreground" Value="White"/>
                    <Setter Property="FontWeight" Value="Bold"/><Setter Property="FontSize" Value="14"/>
                    <Setter Property="Padding" Value="12,10"/><Setter Property="HorizontalContentAlignment" Value="Center"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>
        </DataGrid>

        <!-- Skeleton Loader -->
        <components:SkeletonLoaderView Grid.RowSpan="2"
                                       Background="White"
                                       RowCount="12"
                                       Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed}"
                                       HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
    </Grid>
</UserControl>
