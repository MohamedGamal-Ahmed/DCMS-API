<Window x:Class="DCMS.WPF.Views.OutboundDetailsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DCMS.WPF.Views"
        mc:Ignorable="d"
        Title="تفاصيل الصادر" Height="700" Width="1000"
        WindowStartupLocation="CenterScreen"
        FlowDirection="RightToLeft"
        Background="#F5F5F5">
    
    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        
        <!-- Timeline Item Template -->
        <DataTemplate x:Key="TimelineItemTemplate">
            <Grid Margin="0,0,0,20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="50"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Icon/Dot -->
                <Border Grid.Column="0" Width="30" Height="30" CornerRadius="15" Background="#2196F3" VerticalAlignment="Top" Margin="0,5,0,0">
                    <TextBlock Text="📅" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White" FontSize="14"/>
                </Border>
                
                <!-- Line -->
                <Border Grid.Column="0" Width="2" Background="#E0E0E0" HorizontalAlignment="Center" Margin="0,35,0,-20" Panel.ZIndex="-1"/>

                <!-- Content -->
                <Border Grid.Column="1" Background="White" CornerRadius="8" Padding="15" Margin="10,0,0,0">
                    <Border.Effect>
                        <DropShadowEffect Color="#E0E0E0" BlurRadius="5" ShadowDepth="1" Opacity="0.5"/>
                    </Border.Effect>
                    <StackPanel>
                        <Grid Margin="0,0,0,5">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding Title}" FontWeight="Bold" FontSize="14" Foreground="#333"/>
                            <TextBlock Grid.Column="1" Text="{Binding Date, StringFormat='dd/MM/yyyy HH:mm'}" Foreground="#999" FontSize="12"/>
                        </Grid>
                        <TextBlock Text="{Binding Description}" TextWrapping="Wrap" Foreground="#666"/>
                        
                        <!-- Attachment Button -->
                        <Button Command="{Binding DataContext.OpenTimelineAttachmentCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                CommandParameter="{Binding}"
                                Visibility="{Binding HasAttachment, Converter={StaticResource BooleanToVisibilityConverter}}"
                                HorizontalAlignment="Left" Margin="0,10,0,0"
                                Background="Transparent" BorderBrush="#2196F3" BorderThickness="1" Padding="10,5" Cursor="Hand">
                            <Button.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="4"/>
                                </Style>
                            </Button.Resources>
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="📎" Margin="0,0,5,0"/>
                                <TextBlock Text="عرض المرفق" Foreground="#2196F3"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Border>
            </Grid>
        </DataTemplate>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header -->
            <RowDefinition Height="*"/>    <!-- Content -->
            <RowDefinition Height="Auto"/> <!-- Footer -->
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Background="White" Padding="20" BorderBrush="#E0E0E0" BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <Border Background="#E3F2FD" CornerRadius="4" Padding="8,4" Margin="0,0,10,0">
                            <TextBlock Text="صادر" Foreground="#1976D2" FontWeight="Bold"/>
                        </Border>
                        <TextBlock Text="{Binding Outbound.SubjectNumber}" FontSize="18" FontWeight="Bold" VerticalAlignment="Center" Foreground="#333"/>
                        <TextBlock Text=" | " FontSize="18" Foreground="#CCC" Margin="10,0"/>
                        <TextBlock Text="{Binding Outbound.Code}" FontSize="16" VerticalAlignment="Center" Foreground="#666"/>
                    </StackPanel>
                    <TextBlock Text="{Binding Outbound.Subject}" FontSize="16" TextWrapping="Wrap" Foreground="#444"/>
                </StackPanel>

                <StackPanel Grid.Column="1" VerticalAlignment="Center">
                    <TextBlock Text="{Binding Outbound.OutboundDate, StringFormat='dd/MM/yyyy'}" FontSize="14" Foreground="#666" HorizontalAlignment="Right"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Content -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.5*"/> <!-- Timeline -->
                <ColumnDefinition Width="30"/>
                <ColumnDefinition Width="2*"/>   <!-- Details -->
            </Grid.ColumnDefinitions>

            <!-- Timeline Section -->
            <Border Grid.Column="0" Background="#FAFAFA" CornerRadius="8" Padding="20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Text="سجل الأحداث" FontSize="16" FontWeight="Bold" Foreground="#2196F3" Margin="0,0,0,15"/>
                    
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <ItemsControl ItemsSource="{Binding Timeline}" ItemTemplate="{StaticResource TimelineItemTemplate}"/>
                    </ScrollViewer>
                </Grid>
            </Border>

            <!-- Details Section -->
            <ScrollViewer Grid.Column="2" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <!-- Basic Info Card -->
                    <Border Background="White" CornerRadius="8" Padding="20" Margin="0,0,0,20">
                        <Border.Effect>
                            <DropShadowEffect Color="#E0E0E0" BlurRadius="8" ShadowDepth="2" Opacity="0.3"/>
                        </Border.Effect>
                        <StackPanel>
                            <TextBlock Text="بيانات أساسية" FontSize="16" FontWeight="Bold" Foreground="#2196F3" Margin="0,0,0,15"/>
                            
                            <Grid Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="جهة / صادر إلى:" Foreground="#666"/>
                                <TextBlock Grid.Column="1" Text="{Binding Outbound.ToEntity}" FontWeight="SemiBold"/>
                            </Grid>

                            <Grid Margin="0,0,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Text="المهندس المسؤول:" Foreground="#666"/>
                                <TextBlock Grid.Column="1" Text="{Binding Outbound.ResponsibleEngineer}" FontWeight="SemiBold"/>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <!-- Linked Inbound -->
                    <Border Background="#E3F2FD" CornerRadius="8" Padding="15" Margin="0,0,0,20"
                            Visibility="{Binding HasInboundLink, Converter={StaticResource BooleanToVisibilityConverter}}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🔗 " FontSize="16"/>
                            <TextBlock Text="{Binding InboundSubject}" VerticalAlignment="Center" Margin="0,0,15,0" FontWeight="SemiBold"/>
                            <Button Command="{Binding GoToInboundCommand}" Content="عرض الوارد" 
                                    Padding="15,5" Background="#1976D2" Foreground="White" BorderThickness="0" Cursor="Hand">
                                <Button.Resources>
                                    <Style TargetType="Border">
                                        <Setter Property="CornerRadius" Value="4"/>
                                    </Style>
                                </Button.Resources>
                            </Button>
                        </StackPanel>
                    </Border>

                    <!-- Attachments Card -->
                    <Border Background="White" CornerRadius="8" Padding="20" Margin="0,0,0,20">
                        <Border.Effect>
                            <DropShadowEffect Color="#E0E0E0" BlurRadius="8" ShadowDepth="2" Opacity="0.3"/>
                        </Border.Effect>
                        <StackPanel>
                            <TextBlock Text="المرفقات" FontSize="16" FontWeight="Bold" Foreground="#2196F3" Margin="0,0,0,15"/>
                            
                            <!-- Original Attachment -->
                            <Border BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4" Padding="10" Margin="0,0,0,10">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <TextBlock Text="📎 مرفق الصادر الأصلي:" FontWeight="SemiBold" Margin="0,0,10,0" VerticalAlignment="Center" Width="130"/>
                                    <TextBox Grid.Column="1" Text="{Binding OriginalAttachmentUrl, UpdateSourceTrigger=PropertyChanged}" 
                                             Padding="8,5" BorderBrush="#CCC" BorderThickness="1" VerticalAlignment="Center" FlowDirection="LeftToRight"/>
                                    <Button Grid.Column="2" Command="{Binding SaveOriginalAttachmentCommand}" Content="💾 حفظ"
                                            Margin="10,0,0,0" Padding="15,5" Background="#4CAF50" Foreground="White" BorderThickness="0" Cursor="Hand">
                                        <Button.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="4"/>
                                            </Style>
                                        </Button.Resources>
                                    </Button>
                                </Grid>
                            </Border>

                            <!-- Response Attachment -->
                            <Border BorderBrush="#E0E0E0" BorderThickness="1" CornerRadius="4" Padding="10">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    
                                    <TextBlock Text="📎 مرفق الرد:" FontWeight="SemiBold" Margin="0,0,10,0" VerticalAlignment="Center" Width="130"/>
                                    <TextBox Grid.Column="1" Text="{Binding ReplyAttachmentUrl, UpdateSourceTrigger=PropertyChanged}" 
                                             Padding="8,5" BorderBrush="#CCC" BorderThickness="1" VerticalAlignment="Center" FlowDirection="LeftToRight"/>
                                    <Button Grid.Column="2" Command="{Binding SaveReplyAttachmentCommand}" Content="💾 حفظ"
                                            Margin="10,0,0,0" Padding="15,5" Background="#4CAF50" Foreground="White" BorderThickness="0" Cursor="Hand">
                                        <Button.Resources>
                                            <Style TargetType="Border">
                                                <Setter Property="CornerRadius" Value="4"/>
                                            </Style>
                                        </Button.Resources>
                                    </Button>
                                </Grid>
                            </Border>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>
        </Grid>

        <!-- Footer Actions -->
        <Border Grid.Row="2" Background="White" Padding="20" BorderBrush="#E0E0E0" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Command="{Binding DeleteCommand}" Content="حذف المراسلة" 
                        Background="#F44336" Foreground="White" Padding="30,10" BorderThickness="0" Margin="0,0,10,0"
                        Visibility="{Binding IsAdmin, Converter={StaticResource BooleanToVisibilityConverter}}">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>
                
                <Button Command="{Binding CloseCommand}" Content="إغلاق" Background="#9E9E9E" Foreground="White" Padding="30,10" BorderThickness="0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</Window>
