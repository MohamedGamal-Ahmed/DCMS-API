<UserControl x:Class="DCMS.WPF.Views.ReportingView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:components="clr-namespace:DCMS.WPF.Views.Components"
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="800"
             FlowDirection="RightToLeft">
    
    <Grid Background="White" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" BorderBrush="#EEEEEE" BorderThickness="0,0,0,2" Padding="0,0,0,20" Margin="0,0,0,20">
            <StackPanel>
                <TextBlock Text="📊 التقارير والإحصائيات" FontSize="26" FontWeight="Bold" Foreground="#2C3E50"/>
                <TextBlock Text="استخراج تقارير PDF رسمية للمراسلات" FontSize="14" Foreground="#7F8C8D" Margin="0,5,0,0"/>
            </StackPanel>
        </Border>

        <!-- Controls -->
        <Border Grid.Row="1" Background="#F9F9F9" CornerRadius="10" Padding="20">
            <StackPanel>
                <TextBlock Text="خيارات التقرير" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15"/>
                
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    
                    <!-- Right Column -->
                    <StackPanel Grid.Column="0">
                        <TextBlock Text="نوع التقرير" FontWeight="Bold" Margin="0,0,0,5"/>
                        <ComboBox ItemsSource="{Binding ReportTypes}" 
                                  SelectedItem="{Binding SelectedReportType}"
                                  Height="35" Padding="5"/>

                        <StackPanel Visibility="{Binding IsInventoryReport, Converter={StaticResource BoolToVisConverter}}">
                            <TextBlock Text="بحث (موضوع / كود / رقم)" FontWeight="Bold" Margin="0,15,0,5"/>
                            <TextBox Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}" Height="35" Padding="5"/>

                            <TextBlock Text="الحالة" FontWeight="Bold" Margin="0,15,0,5"/>
                            <ComboBox ItemsSource="{Binding Statuses}" 
                                      SelectedItem="{Binding SelectedStatus}"
                                      Height="35" Padding="5" IsEditable="False"/>
                        </StackPanel>

                        <StackPanel Visibility="{Binding IsTransmittalReport, Converter={StaticResource BoolToVisConverter}}">
                             <TextBlock Text="اسم المستلم (لتقرير التسليم)" FontWeight="Bold" Margin="0,15,0,5"/>
                             <TextBox Text="{Binding RecipientName}" Height="35" Padding="5"/>
                        </StackPanel>
                    </StackPanel>
                    
                    <!-- Left Column -->
                    <StackPanel Grid.Column="2">
                        <TextBlock Text="من تاريخ" FontWeight="Bold" Margin="0,0,0,5"/>
                        <DatePicker SelectedDate="{Binding FromDate}" Height="35"/>

                        <TextBlock Text="إلى تاريخ" FontWeight="Bold" Margin="0,15,0,5"/>
                        <DatePicker SelectedDate="{Binding ToDate}" Height="35"/>
                    </StackPanel>
                </Grid>

                <Button Command="{Binding PreviewReportCommand}"
                        Content="🔍 بحث / معاينة" 
                        Margin="0,30,0,0"
                        Height="45"
                        FontSize="16"
                        FontWeight="Bold"
                        Background="#2980B9"
                        Foreground="White"
                        Cursor="Hand"
                        HorizontalAlignment="Center"
                        Padding="30,0">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="5"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>

        <!-- Data Preview -->
        <Border Grid.Row="2" Background="White" BorderBrush="#EEEEEE" BorderThickness="1" CornerRadius="5" Margin="0,20,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Toolbar -->
                <Border Grid.Row="0" Background="#F9F9F9" Padding="10" BorderBrush="#EEEEEE" BorderThickness="0,0,0,1">
                    <DockPanel>
                        <TextBlock Text="معاينة البيانات" VerticalAlignment="Center" FontWeight="Bold" Foreground="#7F8C8D"/>
                        <Button Command="{Binding ExportReportCommand}" 
                                Content="💾 تصدير PDF" 
                                HorizontalAlignment="Left"
                                IsEnabled="{Binding HasData}"
                                Background="#27AE60" BorderBrush="#27AE60"/>
                    </DockPanel>
                </Border>

                <!-- DataGrid -->
                <DataGrid Grid.Row="1" 
                          ItemsSource="{Binding ReportData}" 
                          AutoGenerateColumns="True" 
                          IsReadOnly="True"
                          HeadersVisibility="Column"
                          GridLinesVisibility="Horizontal"
                          Background="White"
                          MinHeight="200"
                          FlowDirection="RightToLeft">
                     <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="Padding" Value="10"/>
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        </Style>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Padding" Value="5"/>
                            <Setter Property="TextBlock.TextAlignment" Value="Center"/>
                        </Style>
                     </DataGrid.Resources>
                </DataGrid>
                
                <!-- Skeleton Loader -->
                <components:SkeletonLoaderView Grid.Row="1" 
                                               Background="White"
                                               RowCount="12"
                                               Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisConverter}}"
                                               HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
            </Grid>
        </Border>
    </Grid>
</UserControl>
