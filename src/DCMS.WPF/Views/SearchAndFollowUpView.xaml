<UserControl x:Class="DCMS.WPF.Views.SearchAndFollowUpView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DCMS.WPF.Views"
             xmlns:enums="clr-namespace:DCMS.Domain.Enums;assembly=DCMS.Domain"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:filters="clr-namespace:DCMS.WPF.Views.SearchFilters"
             xmlns:components="clr-namespace:DCMS.WPF.Views.SearchComponents"
             xmlns:vm="clr-namespace:DCMS.WPF.ViewModels"
             xmlns:helpers="clr-namespace:DCMS.WPF.Helpers"
             mc:Ignorable="d" 
             d:DesignHeight="800" d:DesignWidth="1400"
             FlowDirection="RightToLeft"
             Background="#F5F5F5">
    
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        
        <ObjectDataProvider x:Key="CorrespondenceStatusEnum" MethodName="GetValues" ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="enums:CorrespondenceStatus"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>

        <ObjectDataProvider x:Key="SearchRecordTypeEnum" MethodName="GetValues" ObjectType="{x:Type sys:Enum}">
            <ObjectDataProvider.MethodParameters>
                <x:Type TypeName="enums:SearchRecordType"/>
            </ObjectDataProvider.MethodParameters>
        </ObjectDataProvider>

        <!-- Binding Proxy for ContextMenus -->
        <helpers:BindingProxy x:Key="Proxy" Data="{Binding}"/>

        <!-- Modern Styles (Keeping here for non-filter elements if needed, though mostly moved) -->
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Padding" Value="10,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#DDD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style x:Key="ModernComboBox" TargetType="ComboBox">
            <Setter Property="Padding" Value="8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#DDD"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Height" Value="36"/>
        </Style>

        <!-- Modern Accent Button Style -->
        <Style x:Key="EmeraldButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#2E7D32"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="6" 
                                BorderThickness="0"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1B5E20"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="AccentButtonStyle" TargetType="Button" BasedOn="{StaticResource EmeraldButtonStyle}">
            <Setter Property="Background" Value="#1565C0"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#0D47A1"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Entry Form DataTemplates -->
        <DataTemplate DataType="{x:Type vm:PostaInboundViewModel}">
            <local:PostaInboundView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:EmailInboundViewModel}">
            <local:EmailInboundView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:RequestInboundViewModel}">
            <local:RequestInboundView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:MissionInboundViewModel}">
            <local:MissionInboundView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:ContractInboundViewModel}">
            <local:ContractInboundView/>
        </DataTemplate>
        <DataTemplate DataType="{x:Type vm:PostaOutboundViewModel}">
            <local:PostaOutboundView/>
        </DataTemplate>
    </UserControl.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header & Type Selector -->
            <RowDefinition Height="Auto"/> <!-- Filters -->
            <RowDefinition Height="Auto"/> <!-- Transferred To Filter -->
            <RowDefinition Height="*"/>    <!-- Results Grid -->
            <RowDefinition Height="Auto"/> <!-- Action Buttons -->
        </Grid.RowDefinitions>

        <!-- Header & Search Type Selector -->
        <Border Grid.Row="0" Background="White" Padding="20,15" Margin="0,0,0,15" CornerRadius="8">
            <Border.Effect>
                <DropShadowEffect Color="#E0E0E0" BlurRadius="8" ShadowDepth="2" Opacity="0.3"/>
            </Border.Effect>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal">
                    <TextBlock Text="🔍 " FontSize="20" VerticalAlignment="Center"/>
                    <TextBlock Text="البحث والمتابعة" FontSize="18" FontWeight="Bold" VerticalAlignment="Center" Foreground="#2196F3"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal">

                    <!-- Quick Add Buttons -->
                    <Button x:Name="btnAddInbound"
                            Content="➕ إضافة وارد" 
                            Style="{StaticResource EmeraldButtonStyle}"
                            Height="38" Margin="0,0,10,0">
                        <Button.ContextMenu>
                            <ContextMenu DataContext="{Binding Data, Source={StaticResource Proxy}}">
                                <MenuItem Header="📮 بوسطة" Command="{Binding OpenInboundFormCommand}" CommandParameter="Posta"/>
                                <MenuItem Header="📧 إيميلات" Command="{Binding OpenInboundFormCommand}" CommandParameter="Email"/>
                                <MenuItem Header="📝 طلبات وشكاوي" Command="{Binding OpenInboundFormCommand}" CommandParameter="Request"/>
                                <MenuItem Header="💼 مأموريات وعهد" Command="{Binding OpenInboundFormCommand}" CommandParameter="Mission"/>
                                <MenuItem Header="📜 عقود" Command="{Binding OpenInboundFormCommand}" CommandParameter="Contract"/>
                            </ContextMenu>
                        </Button.ContextMenu>
                        <Button.Triggers>
                            <EventTrigger RoutedEvent="Button.Click">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <BooleanAnimationUsingKeyFrames Storyboard.TargetProperty="(Button.ContextMenu).IsOpen">
                                            <DiscreteBooleanKeyFrame KeyTime="0:0:0" Value="True"/>
                                        </BooleanAnimationUsingKeyFrames>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Button.Triggers>
                    </Button>

                    <Button Content="➕ إضافة صادر" 
                            Command="{Binding OpenOutboundFormCommand}"
                            Style="{StaticResource AccentButtonStyle}"
                            Height="38" Margin="0,0,20,0"/>

                    <TextBlock Text="نوع المراسلة:" VerticalAlignment="Center" Margin="0,0,10,0" FontWeight="SemiBold"/>
                    <ComboBox x:Name="cmbRecordType" SelectedValue="{Binding SelectedRecordType}" SelectedValuePath="Tag"
                              Width="220" Style="{StaticResource ModernComboBox}">
                        <ComboBoxItem Content="بوسطة وارد" Tag="{x:Static enums:SearchRecordType.Posta}"/>
                        <ComboBoxItem Content="إيميل" Tag="{x:Static enums:SearchRecordType.Email}"/>
                        <ComboBoxItem Content="عقود" Tag="{x:Static enums:SearchRecordType.Contract}"/>
                        <ComboBoxItem Content="تفويضات" Tag="{x:Static enums:SearchRecordType.Delegation}"/>
                        <ComboBoxItem Content="عهدة" Tag="{x:Static enums:SearchRecordType.Custody}"/>
                        <ComboBoxItem Content="مأموريات" Tag="{x:Static enums:SearchRecordType.Mission}"/>
                        <ComboBoxItem Content="طلبات" Tag="{x:Static enums:SearchRecordType.Request}"/>
                        <ComboBoxItem Content="شكاوى" Tag="{x:Static enums:SearchRecordType.Complaint}"/>
                        <ComboBoxItem Content="صادر" Tag="{x:Static enums:SearchRecordType.Outbound}"/>
                    </ComboBox>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Dynamic Filters Section -->
        <Border Grid.Row="1" Background="White" Padding="20" Margin="0,0,0,15" CornerRadius="8">
            <Border.Effect>
                <DropShadowEffect Color="#E0E0E0" BlurRadius="8" ShadowDepth="2" Opacity="0.3"/>
            </Border.Effect>
            
            <!-- ContentControl with dynamic control type selection -->
            <ContentControl Content="{Binding}">
                <ContentControl.Style>
                    <Style TargetType="ContentControl">
                        <Setter Property="ContentTemplate">
                            <Setter.Value>
                                <DataTemplate>
                                    <filters:DefaultFiltersControl/>
                                </DataTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <!-- Outbound -->
                            <DataTrigger Binding="{Binding SelectedRecordType}" Value="{x:Static enums:SearchRecordType.Outbound}">
                                <Setter Property="ContentTemplate">
                                    <Setter.Value>
                                        <DataTemplate>
                                            <filters:OutboundFiltersControl/>
                                        </DataTemplate>
                                    </Setter.Value>
                                </Setter>
                            </DataTrigger>
                            
                            <!-- Inbound Categories -->
                            <DataTrigger Binding="{Binding SelectedRecordType}" Value="{x:Static enums:SearchRecordType.Posta}">
                                <Setter Property="ContentTemplate">
                                    <Setter.Value>
                                        <DataTemplate>
                                            <filters:PostaFiltersControl/>
                                        </DataTemplate>
                                    </Setter.Value>
                                </Setter>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding SelectedRecordType}" Value="{x:Static enums:SearchRecordType.Email}">
                                <Setter Property="ContentTemplate">
                                    <Setter.Value>
                                        <DataTemplate>
                                            <filters:EmailFiltersControl/>
                                        </DataTemplate>
                                    </Setter.Value>
                                </Setter>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding SelectedRecordType}" Value="{x:Static enums:SearchRecordType.Contract}">
                                <Setter Property="ContentTemplate">
                                    <Setter.Value>
                                        <DataTemplate>
                                            <filters:ContractFiltersControl/>
                                        </DataTemplate>
                                    </Setter.Value>
                                </Setter>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </ContentControl.Style>
            </ContentControl>
        </Border>

        <!-- Year Filter & Transferred To Filter Section -->
        <Border Grid.Row="2" Background="White" Padding="15,10" Margin="0,0,0,10" CornerRadius="8">
            <Border.Effect>
                <DropShadowEffect Color="#E0E0E0" BlurRadius="6" ShadowDepth="1" Opacity="0.2"/>
            </Border.Effect>
            
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <!-- Year Filter Row -->
                <Grid Grid.Row="0" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="300"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Grid.Column="0" Text="📅 السنة:" VerticalAlignment="Center" 
                               Margin="0,0,15,0" FontWeight="SemiBold" FontSize="14"/>
                    <ComboBox Grid.Column="1" 
                              ItemsSource="{Binding AvailableYears}"
                              SelectedItem="{Binding SelectedYear}"
                              Style="{StaticResource ModernComboBox}"
                              ToolTip="اختر السنة لعرض مراسلات تلك السنة فقط">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <TextBlock>
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Text" Value="{Binding}"/>
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding}" Value="0">
                                                    <Setter Property="Text" Value="جميع السنوات"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>

                    <TextBlock Grid.Column="2" Text="🔄 المحول إليه:" VerticalAlignment="Center" 
                               Margin="30,0,15,0" FontWeight="SemiBold" FontSize="14"/>
                    <ComboBox Grid.Column="3" 
                              ItemsSource="{Binding AvailableEngineers}"
                              SelectedValue="{Binding SearchTransferredTo}"
                              SelectedValuePath="FullName"
                              DisplayMemberPath="FullName"
                              Style="{StaticResource ModernComboBox}"
                              IsEditable="True"
                              IsTextSearchEnabled="True"
                              ToolTip="البحث عن جميع الموضوعات المحولة حالياً لمهندس معين (غير المغلقة)"/>
                    
                    <TextBlock Grid.Column="4" Text="(جميع السنوات محددة افتراضياً)" 
                               VerticalAlignment="Center" Margin="15,0,0,0" 
                               Foreground="#666" FontSize="12" FontStyle="Italic"/>
                </Grid>
            </Grid>
        </Border>

        <!-- Results DataGrid -->
        <Border Grid.Row="3" Background="White" CornerRadius="8" Padding="0">
            <Border.Effect>
                <DropShadowEffect Color="#E0E0E0" BlurRadius="8" ShadowDepth="2" Opacity="0.3"/>
            </Border.Effect>
            <components:SearchResultsControl/>
        </Border>


        <!-- Action Buttons -->
        <components:SearchFooterControl Grid.Row="4" Margin="0,15,0,0"/>

        <!-- Modal Overlay -->
        <Grid Grid.Row="0" Grid.RowSpan="5" Background="#80000000" Panel.ZIndex="1000">
            <Grid.Style>
                <Style TargetType="Grid">
                    <Setter Property="Visibility" Value="Collapsed"/>
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding ActiveEntryViewModel, Converter={StaticResource EqualityConverter}, ConverterParameter={x:Null}}" Value="False">
                            <Setter Property="Visibility" Value="Visible"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </Grid.Style>

            <Border Background="White" CornerRadius="12" 
                    MaxWidth="980" 
                    MaxHeight="{Binding ActualHeight, RelativeSource={RelativeSource AncestorType=Grid}, Converter={StaticResource PercentageConverter}, ConverterParameter=0.9}"
                    Margin="20"
                    VerticalAlignment="Center" HorizontalAlignment="Stretch" Padding="0">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="40" ShadowDepth="0" Opacity="0.4"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="65"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="#F3F4F6" CornerRadius="12,12,0,0" Padding="25,10" BorderBrush="#E5E7EB" BorderThickness="0,0,0,1">
                        <Grid>
                            <TextBlock Text="{Binding ActiveEntryViewModel.Title, FallbackValue='إضافة مراسلة جديدة'}" 
                                       Foreground="#1F2937" FontSize="20" FontWeight="Bold" HorizontalAlignment="Right" VerticalAlignment="Center"/>
                            <Button HorizontalAlignment="Left" VerticalAlignment="Center" Background="Transparent" BorderThickness="0" 
                                    Command="{Binding CloseEntryFormCommand}" Cursor="Hand">
                                <TextBlock Text="✕" Foreground="#EF4444" FontSize="24" FontWeight="Bold"/>
                            </Button>
                        </Grid>
                    </Border>

                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="20" Background="#FAFAFA">
                        <ContentControl Content="{Binding ActiveEntryViewModel}">
                            <ContentControl.Resources>
                                <DataTemplate DataType="{x:Type vm:PostaInboundViewModel}">
                                    <local:PostaInboundView/>
                                </DataTemplate>
                                <DataTemplate DataType="{x:Type vm:EmailInboundViewModel}">
                                    <local:EmailInboundView/>
                                </DataTemplate>
                                <DataTemplate DataType="{x:Type vm:RequestInboundViewModel}">
                                    <local:RequestInboundView/>
                                </DataTemplate>
                                <DataTemplate DataType="{x:Type vm:MissionInboundViewModel}">
                                    <local:MissionInboundView/>
                                </DataTemplate>
                                <DataTemplate DataType="{x:Type vm:ContractInboundViewModel}">
                                    <local:ContractInboundView/>
                                </DataTemplate>
                                <DataTemplate DataType="{x:Type vm:PostaOutboundViewModel}">
                                    <local:PostaOutboundView/>
                                </DataTemplate>
                            </ContentControl.Resources>
                        </ContentControl>
                    </ScrollViewer>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
