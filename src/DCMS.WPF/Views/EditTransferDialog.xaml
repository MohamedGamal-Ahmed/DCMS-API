<Window x:Class="DCMS.WPF.Views.EditTransferDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="تعديل سجل التحويل/الأحداث" Height="500" Width="600"
        WindowStartupLocation="CenterScreen"
        FlowDirection="RightToLeft"
        Background="#F4F7F9">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header -->
            <RowDefinition Height="*"/>    <!-- Form Content -->
            <RowDefinition Height="Auto"/> <!-- Buttons -->
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,20">
            <TextBlock Text="{Binding EngineerName, StringFormat='تعديل بيانات المهندس: {0}'}" FontSize="18" FontWeight="Bold" Foreground="#2C3E50"/>
            <Separator Margin="0,10,0,0"/>
        </StackPanel>

        <!-- Form Content -->
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <!-- Transfer Details -->
                <TextBlock Text="بيانات التحويل" FontWeight="SemiBold" Margin="0,0,0,10" Foreground="#2196F3"/>
                
                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="تاريخ التحويل:" VerticalAlignment="Center" Margin="0,0,15,10"/>
                    <DatePicker Grid.Row="0" Grid.Column="1" SelectedDate="{Binding TransferDate}" Margin="0,0,0,10" Padding="5"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="رابط التأشيرة:" VerticalAlignment="Center" Margin="0,0,15,0"/>
                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding TransferAttachmentUrl}" Padding="5" Height="30"/>
                </Grid>

                <Separator Margin="0,10,0,20"/>

                <!-- Response Details -->
                <TextBlock Text="بيانات الرد (إن وجد)" FontWeight="SemiBold" Margin="0,0,0,10" Foreground="#2196F3"/>

                <TextBlock Text="الرد:" Margin="0,0,0,5"/>
                <TextBox Text="{Binding Response}" AcceptsReturn="True" TextWrapping="Wrap" MinHeight="80" Margin="0,0,0,15" Padding="5"/>

                <Grid Margin="0,0,0,15">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Grid.Column="0" Text="تاريخ الرد:" VerticalAlignment="Center" Margin="0,0,15,10"/>
                    <DatePicker Grid.Row="0" Grid.Column="1" SelectedDate="{Binding ResponseDate}" Margin="0,0,0,10" Padding="5"/>

                    <TextBlock Grid.Row="1" Grid.Column="0" Text="رابط الرد:" VerticalAlignment="Center" Margin="0,0,15,0"/>
                    <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding ResponseAttachmentUrl}" Padding="5" Height="30"/>
                </Grid>
            </StackPanel>
        </ScrollViewer>

        <!-- Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,20,0,0">
            <Button Command="{Binding SaveCommand}" Content="💾 حفظ التعديلات" IsDefault="True" Width="140" Padding="15,10" Background="#4CAF50" Foreground="White" BorderThickness="0" FontWeight="SemiBold">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="4"/>
                    </Style>
                </Button.Resources>
            </Button>
            
            <Button Command="{Binding DeleteCommand}" Content="🗑️ حذف السجل" Width="140" Padding="15,10" Margin="10,0,0,0" Background="#F44336" Foreground="White" BorderThickness="0" FontWeight="SemiBold">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="4"/>
                    </Style>
                </Button.Resources>
            </Button>

            <Button Command="{Binding CancelCommand}" Content="إلغاء" IsCancel="True" Width="100" Padding="15,10" Margin="10,0,0,0" Background="#9E9E9E" Foreground="White" BorderThickness="0">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="4"/>
                    </Style>
                </Button.Resources>
            </Button>
        </StackPanel>
    </Grid>
</Window>
