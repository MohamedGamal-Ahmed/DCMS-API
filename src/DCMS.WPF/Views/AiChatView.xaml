<UserControl x:Class="DCMS.WPF.Views.AiChatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:DCMS.WPF.Views"
             xmlns:vm="clr-namespace:DCMS.WPF.ViewModels"
             xmlns:md="clr-namespace:Markdig.Wpf;assembly=Markdig.Wpf"
             xmlns:components="clr-namespace:DCMS.WPF.Views.Components"
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1100"
             Background="#F5F7FA">
    
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        <local:BooleanToAlignmentConverter x:Key="BooleanToAlignmentConverter"/>
        <local:ConnectionStatusToColorConverter x:Key="ConnectionStatusToColorConverter"/>
        <local:ConnectionStatusToTextConverter x:Key="ConnectionStatusToTextConverter"/>
        <local:BooleanToChatColorConverter x:Key="BooleanToChatColorConverter"/>
        <local:BooleanToHeaderColorConverter x:Key="BooleanToHeaderColorConverter"/>
        <local:RoleToTextConverter x:Key="RoleToTextConverter"/>
        <local:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
        <local:NullToBooleanConverter x:Key="NullToBooleanConverter"/>
        <local:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter"/>
        <local:UserInitialsConverter x:Key="UserInitialsConverter"/>
        <local:InitialsToColorConverter x:Key="InitialsToColorConverter"/>
        <local:IntToVisibilityConverter x:Key="IntToVisibilityConverter"/>
        
        
        <!-- Stats Card Style -->
        <Style x:Key="StatsCardStyle" TargetType="Border">
            <Setter Property="Background" Value="White"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Padding" Value="15"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#E0E0E0" BlurRadius="10" ShadowDepth="2" Opacity="0.3"/>
                </Setter.Value>
            </Setter>
        </Style>
        
        <!-- Yellow Alert Card Style -->
        <Style x:Key="YellowAlertCardStyle" TargetType="Border">
            <Setter Property="Background" Value="#FFF9E6"/>
            <Setter Property="BorderBrush" Value="#F1C40F"/>
            <Setter Property="BorderThickness" Value="3,0,0,0"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>
        
        <!-- Red Alert Card Style -->
        <Style x:Key="RedAlertCardStyle" TargetType="Border">
            <Setter Property="Background" Value="#FDEDEC"/>
            <Setter Property="BorderBrush" Value="#E74C3C"/>
            <Setter Property="BorderThickness" Value="3,0,0,0"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>
        
        <!-- Orange Alert Card Style (Missing Attachments) -->
        <Style x:Key="OrangeAlertCardStyle" TargetType="Border">
            <Setter Property="Background" Value="#FEF5E7"/>
            <Setter Property="BorderBrush" Value="#E67E22"/>
            <Setter Property="BorderThickness" Value="3,0,0,0"/>
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="12"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>
        
        <!-- Interactive Button Style -->
        <Style x:Key="InteractiveButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#3498DB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="12,6"/>
            <Setter Property="Margin" Value="0,5,5,0"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="15" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#2980B9"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>  <!-- Header -->
            <RowDefinition Height="Auto"/>  <!-- Stats Cards -->
            <RowDefinition Height="*"/>     <!-- Main Content -->
        </Grid.RowDefinitions>
        
        <!-- Row 0: Header -->
        <Border Grid.Row="0" Padding="20,15">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                    <GradientStop Color="#2C3E50" Offset="0"/>
                    <GradientStop Color="#3498DB" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
            <Grid>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                    <TextBlock Text="🤖" FontSize="28" Margin="0,0,12,0" VerticalAlignment="Center"/>
                    <StackPanel>
                        <TextBlock Text="مركز قيادة المتابعة الفنية" Foreground="White" FontWeight="Bold" FontSize="20"/>
                        <TextBlock Text="Technical Follow-up Command Center" Foreground="#BDC3C7" FontSize="11"/>
                    </StackPanel>
                </StackPanel>
                
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button Content="🔍 تطابق الأسماء" 
                            Command="{Binding DebugMatchCommand}"
                            Visibility="{Binding IsAdmin, Converter={StaticResource BooleanToVisibilityConverter}}"
                            Padding="12,6" Margin="0,0,10,0"
                            Background="#F8F9FA" BorderBrush="#DEE2E6" BorderThickness="1">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="15"/>
                            </Style>
                        </Button.Resources>
                    </Button>

                    <Button Content="📊 فحص البيانات" 
                            Command="{Binding ToggleDiagnosticsCommand}"
                            Padding="12,6" Margin="0,0,15,0"
                            Background="#F8F9FA" BorderBrush="#DEE2E6" BorderThickness="1">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="15"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                    
                    <Border Background="#273C75" CornerRadius="15" Padding="15,5">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding UserRole}" FontWeight="Bold" Foreground="White"/>
                            <TextBlock Text=" :مستوى الوصول" Margin="5,0,0,0" Foreground="#BDC3C7"/>
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>
        
        <!-- Row 1: Stats Cards -->
        <UniformGrid Grid.Row="1" Columns="4" Margin="10,10,10,5">
            <!-- Total Internal Transactions -->
            <Border Style="{StaticResource StatsCardStyle}">
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <Ellipse Width="35" Height="35" Fill="#3498DB"/>
                        <TextBlock Text="📋" FontSize="18" Margin="-27,8,0,0"/>
                    </StackPanel>
                    <TextBlock Text="إجمالي المعاملات الداخلية" Foreground="#7F8C8D" FontSize="11" Margin="0,8,0,2"/>
                    <TextBlock Text="{Binding TotalInternalTransactions}" FontSize="24" FontWeight="Bold" Foreground="#2C3E50"/>
                </StackPanel>
            </Border>
            
            <!-- Critical External Delays -->
            <Border Style="{StaticResource StatsCardStyle}">
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <Ellipse Width="35" Height="35" Fill="#E74C3C"/>
                        <TextBlock Text="🔔" FontSize="18" Margin="-27,8,0,0"/>
                    </StackPanel>
                    <TextBlock Text="متأخرات خارجية (حرجة)" Foreground="#7F8C8D" FontSize="11" Margin="0,8,0,2"/>
                    <TextBlock Text="{Binding CriticalExternalDelays}" FontSize="24" FontWeight="Bold" Foreground="#E74C3C"/>
                </StackPanel>
            </Border>
            
            <!-- Fastest Engineer -->
            <Border Style="{StaticResource StatsCardStyle}">
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <Ellipse Width="35" Height="35" Fill="#F39C12"/>
                        <TextBlock Text="⭐" FontSize="18" Margin="-27,8,0,0"/>
                    </StackPanel>
                    <TextBlock Text="أسرع مهندس" Foreground="#7F8C8D" FontSize="11" Margin="0,8,0,2"/>
                    <TextBlock Text="{Binding FastestEngineer}" FontSize="16" FontWeight="Bold" Foreground="#2C3E50" TextTrimming="CharacterEllipsis"/>
                </StackPanel>
            </Border>
            
            <!-- Overall Completion Rate -->
            <Border Style="{StaticResource StatsCardStyle}">
                <StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <Ellipse Width="35" Height="35" Fill="#27AE60"/>
                        <TextBlock Text="📊" FontSize="18" Margin="-27,8,0,0"/>
                    </StackPanel>
                    <TextBlock Text="معدل الإنجاز العام" Foreground="#7F8C8D" FontSize="11" Margin="0,8,0,2"/>
                    <TextBlock FontSize="24" FontWeight="Bold" Foreground="#27AE60">
                        <Run Text="{Binding OverallCompletionRate}"/><Run Text="%"/>
                    </TextBlock>
                </StackPanel>
            </Border>
        </UniformGrid>
        
        <!-- Row 2: Main Content - 3 Columns (role-based visibility) -->
        <Grid Grid.Row="2" Margin="10,5,10,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>  <!-- External Follow-up (Red) -->
                <ColumnDefinition Width="1*"/>  <!-- Internal Review (Yellow) -->
                <ColumnDefinition Width="1*"/>  <!-- Missing Attachments (Orange) -->
            </Grid.ColumnDefinitions>
            
            <!-- Column 0: External Follow-up (Red Theme) - TechnicalManager/Admin only -->
            <Border Grid.Column="0" Background="White" CornerRadius="12" Margin="0,0,5,0"
                    Visibility="{Binding ShowTechnicalColumns, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Visible}">
                <Border.Effect>
                    <DropShadowEffect Color="#E0E0E0" BlurRadius="8" ShadowDepth="2" Opacity="0.3"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Header -->
                    <Border Grid.Row="0" Background="#FDEDEC" CornerRadius="12,12,0,0" Padding="15">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🔴" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBlock Text="موضوعات بانتظار رد الاستشاري" FontWeight="Bold" FontSize="14" Foreground="#E74C3C"/>
                        </StackPanel>
                    </Border>
                    
                    <!-- List -->
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="10">
                        <ItemsControl ItemsSource="{Binding PendingConsultantResponse}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Command="{Binding DataContext.CardClickCommand, RelativeSource={RelativeSource AncestorType=local:AiChatView}}"
                                            CommandParameter="{Binding}" Background="Transparent" BorderThickness="0" Cursor="Hand" HorizontalContentAlignment="Stretch">
                                        <Border Style="{StaticResource RedAlertCardStyle}">
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                </Grid.RowDefinitions>
                                                <!-- Top Row: SubjectNumber (Large Bold) -->
                                                <Grid Grid.Row="0" Margin="0,0,0,8">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Grid.Column="1" Text="{Binding SubjectNumber}" FontWeight="Bold" FontSize="16" Foreground="#C0392B"/>
                                                    <TextBlock Grid.Column="0" Text="🔔" FontSize="18" VerticalAlignment="Center" Foreground="#E74C3C"/>
                                                </Grid>
                                                <!-- Inner Content -->
                                                <StackPanel Grid.Row="1">
                                                    <TextBlock Text="{Binding Subject}" FontWeight="SemiBold" TextWrapping="Wrap" MaxHeight="40" TextTrimming="CharacterEllipsis" Foreground="#2C3E50"/>
                                                    <TextBlock Text="{Binding ResponsibleEngineer, StringFormat='المسئول: {0}'}" FontSize="12" Foreground="#34495E" FontWeight="SemiBold" Margin="0,4,0,0"/>
                                                    <TextBlock Text="{Binding TransferredTo, StringFormat='محولة إلى: {0}'}" FontSize="12" Foreground="#7F8C8D" Margin="0,4,0,0"/>
                                                    <TextBlock Text="{Binding DelayType}" FontSize="12" Foreground="#E74C3C" FontWeight="Bold" Margin="0,4,0,0"/>
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>
            
            <!-- Column 1: Internal Review (Yellow Theme) - TechnicalManager/Admin only -->
            <Border Grid.Column="1" Background="White" CornerRadius="12" Margin="5,0,5,0"
                    Visibility="{Binding ShowTechnicalColumns, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Visible}">
                <Border.Effect>
                    <DropShadowEffect Color="#E0E0E0" BlurRadius="8" ShadowDepth="2" Opacity="0.3"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Header -->
                    <Border Grid.Row="0" Background="#FEF9E7" CornerRadius="12,12,0,0" Padding="15">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🟡" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBlock Text="موضوعات بانتظار العرض على المدير" FontWeight="Bold" FontSize="14" Foreground="#F39C12"/>
                        </StackPanel>
                    </Border>
                    
                    <!-- List -->
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="10">
                        <ItemsControl ItemsSource="{Binding PendingManagerReview}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Command="{Binding DataContext.CardClickCommand, RelativeSource={RelativeSource AncestorType=local:AiChatView}}"
                                            CommandParameter="{Binding}" Background="Transparent" BorderThickness="0" Cursor="Hand" HorizontalContentAlignment="Stretch">
                                        <Border Style="{StaticResource YellowAlertCardStyle}">
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                </Grid.RowDefinitions>
                                                <!-- Top Row: SubjectNumber (Large Bold) -->
                                                <Grid Grid.Row="0" Margin="0,0,0,8">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Grid.Column="1" Text="{Binding SubjectNumber}" FontWeight="Bold" FontSize="16" Foreground="#D68910"/>
                                                    <TextBlock Grid.Column="0" Text="📋" FontSize="18" VerticalAlignment="Center"/>
                                                </Grid>
                                                <!-- Inner Content -->
                                                <StackPanel Grid.Row="1">
                                                    <TextBlock Text="{Binding Subject}" FontWeight="SemiBold" TextWrapping="Wrap" MaxHeight="40" TextTrimming="CharacterEllipsis" Foreground="#2C3E50"/>
                                                    <TextBlock Text="{Binding ResponsibleEngineer, StringFormat='المسئول: {0}'}" FontSize="12" Foreground="#34495E" FontWeight="SemiBold" Margin="0,4,0,0"/>
                                                    <TextBlock Text="{Binding DelayType}" FontSize="12" Foreground="#F39C12" FontWeight="Bold" Margin="0,4,0,0"/>
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </Grid>
            </Border>
            
            <!-- Column 2: Missing Attachments (Orange Theme) - FollowUpStaff/Admin only -->
            <Border Grid.Column="2" Background="White" CornerRadius="12" Margin="5,0,5,0"
                    Visibility="{Binding ShowFollowUpColumns, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Visible}">
                <Border.Effect>
                    <DropShadowEffect Color="#E0E0E0" BlurRadius="8" ShadowDepth="2" Opacity="0.3"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <!-- Header -->
                    <Border Grid.Row="0" Background="#FEF5E7" CornerRadius="12,12,0,0" Padding="15">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🟠" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBlock Text="موضوعات بدون رابط OneDrive" FontWeight="Bold" FontSize="14" Foreground="#E67E22"/>
                        </StackPanel>
                    </Border>
                    
                    <!-- List -->
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="10">
                        <ItemsControl ItemsSource="{Binding MissingAttachments}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Command="{Binding DataContext.CardClickCommand, RelativeSource={RelativeSource AncestorType=local:AiChatView}}"
                                            CommandParameter="{Binding}" Background="Transparent" BorderThickness="0" Cursor="Hand" HorizontalContentAlignment="Stretch">
                                        <Border Style="{StaticResource OrangeAlertCardStyle}">
                                            <Grid>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                </Grid.RowDefinitions>
                                                <!-- Top Row: SubjectNumber (Large Bold) -->
                                                <Grid Grid.Row="0" Margin="0,0,0,8">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="*"/>
                                                        <ColumnDefinition Width="Auto"/>
                                                    </Grid.ColumnDefinitions>
                                                    <TextBlock Grid.Column="1" Text="{Binding SubjectNumber}" FontWeight="Bold" FontSize="16" Foreground="#D35400"/>
                                                    <TextBlock Grid.Column="0" Text="📁" FontSize="18" VerticalAlignment="Center" Foreground="#E67E22"/>
                                                </Grid>
                                                <!-- Inner Content -->
                                                <StackPanel Grid.Row="1">
                                                    <TextBlock Text="{Binding Subject}" FontWeight="SemiBold" TextWrapping="Wrap" MaxHeight="40" TextTrimming="CharacterEllipsis" Foreground="#2C3E50"/>
                                                    <TextBlock Text="{Binding ResponsibleEngineer, StringFormat='المسئول: {0}'}" FontSize="12" Foreground="#34495E" FontWeight="SemiBold" Margin="0,4,0,0"/>
                                                    <TextBlock Text="⚠️ بانتظار رابط OneDrive" FontSize="12" Foreground="#E67E22" FontWeight="Bold" Margin="0,4,0,0"/>
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                    </Grid>
                </Border>
                <!-- Chat column removed - now a floating popup -->
        </Grid>

        <!-- Skeleton Overlay (Visible when IsBusy) -->
        <Grid Grid.RowSpan="3" Panel.ZIndex="1" Background="#F5F7FA" Visibility="{Binding IsBusy, Converter={StaticResource BooleanToVisibilityConverter}}">
             <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>  <!-- Header -->
                <RowDefinition Height="Auto"/>  <!-- Stats Cards -->
                <RowDefinition Height="*"/>     <!-- Main Content -->
            </Grid.RowDefinitions>

            <!-- Header Skeleton -->
            <Border Grid.Row="0" Padding="20,15" Background="#2C3E50" Height="80">
                 <Grid>
                    <StackPanel Orientation="Horizontal">
                        <Border Width="40" Height="40" CornerRadius="20" Background="#34495E"/>
                        <StackPanel Margin="10,0,0,0" VerticalAlignment="Center">
                             <Border Width="200" Height="20" CornerRadius="4" Background="#34495E" Margin="0,0,0,5"/>
                             <Border Width="150" Height="10" CornerRadius="4" Background="#34495E"/>
                        </StackPanel>
                    </StackPanel>
                 </Grid>
            </Border>

            <!-- Stats Skeleton -->
            <UniformGrid Grid.Row="1" Columns="4" Margin="10,10,10,5">
                 <Border Height="100" CornerRadius="12" Margin="5" Background="White">
                     <StackPanel VerticalAlignment="Center" Margin="15">
                         <components:SkeletonLoaderView RowCount="1" Height="20" Width="40" HorizontalAlignment="Left" Margin="0,0,0,10"/>
                         <components:SkeletonLoaderView RowCount="1" Height="15" Width="100" HorizontalAlignment="Left"/>
                     </StackPanel>
                 </Border>
                 <Border Height="100" CornerRadius="12" Margin="5" Background="White">
                     <StackPanel VerticalAlignment="Center" Margin="15">
                         <components:SkeletonLoaderView RowCount="1" Height="20" Width="40" HorizontalAlignment="Left" Margin="0,0,0,10"/>
                         <components:SkeletonLoaderView RowCount="1" Height="15" Width="100" HorizontalAlignment="Left"/>
                     </StackPanel>
                 </Border>
                 <Border Height="100" CornerRadius="12" Margin="5" Background="White">
                     <StackPanel VerticalAlignment="Center" Margin="15">
                         <components:SkeletonLoaderView RowCount="1" Height="20" Width="40" HorizontalAlignment="Left" Margin="0,0,0,10"/>
                         <components:SkeletonLoaderView RowCount="1" Height="15" Width="100" HorizontalAlignment="Left"/>
                     </StackPanel>
                 </Border>
                 <Border Height="100" CornerRadius="12" Margin="5" Background="White">
                     <StackPanel VerticalAlignment="Center" Margin="15">
                         <components:SkeletonLoaderView RowCount="1" Height="20" Width="40" HorizontalAlignment="Left" Margin="0,0,0,10"/>
                         <components:SkeletonLoaderView RowCount="1" Height="15" Width="100" HorizontalAlignment="Left"/>
                     </StackPanel>
                 </Border>
            </UniformGrid>

            <!-- Columns Skeleton -->
            <Grid Grid.Row="2" Margin="10,5,10,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1*"/>
                    <ColumnDefinition Width="1.5*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Background="White" CornerRadius="12" Margin="0,0,5,0">
                     <StackPanel Margin="10">
                         <Border Height="40" Background="#F2F2F2" CornerRadius="8" Margin="0,0,0,10"/> 
                         <components:SkeletonLoaderView RowCount="6" Height="300" />
                     </StackPanel>
                </Border>
                <Border Grid.Column="1" Background="White" CornerRadius="12" Margin="5,0,5,0">
                     <StackPanel Margin="10">
                         <Border Height="40" Background="#F2F2F2" CornerRadius="8" Margin="0,0,0,10"/>
                         <components:SkeletonLoaderView RowCount="6" Height="300" />
                     </StackPanel>
                </Border>
                <Border Grid.Column="2" Background="White" CornerRadius="12" Margin="5,0,5,0">
                     <StackPanel Margin="10">
                         <Border Height="40" Background="#F2F2F2" CornerRadius="8" Margin="0,0,0,10"/>
                         <components:SkeletonLoaderView RowCount="6" Height="300" />
                     </StackPanel>
                </Border>
                <Border Grid.Column="3" Background="White" CornerRadius="12" Margin="5,0,0,0">
                     <StackPanel Margin="10">
                         <Border Height="40" Background="#F2F2F2" CornerRadius="8" Margin="0,0,0,10"/>
                         <components:SkeletonLoaderView RowCount="10" Height="400" />
                     </StackPanel>
                </Border>
            </Grid>
        </Grid>

        <!-- Diagnostic Overlay Panel -->
        <Border Visibility="{Binding ShowDiagnostics, Converter={StaticResource BooleanToVisibilityConverter}}"
                Grid.RowSpan="3" Panel.ZIndex="1000"
                Background="#AA000000" Padding="50">
            <Border Background="White" CornerRadius="15" MaxWidth="550" MaxHeight="600" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Border.Effect>
                    <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.5"/>
                </Border.Effect>
                <Grid Margin="25">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <StackPanel Grid.Row="0" Margin="0,0,0,15">
                        <TextBlock Text="📊 تقرير فحص البيانات والفلترة المتقدمة" FontSize="18" FontWeight="Bold" Foreground="#2C3E50"/>
                        <TextBlock Text="Diagnostic Data &amp; Filtering Report" FontSize="12" Foreground="#7F8C8D"/>
                    </StackPanel>
                    
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <Border Background="#F8F9FA" Padding="20" CornerRadius="12" BorderBrush="#DEE2E6" BorderThickness="1">
                            <TextBlock Text="{Binding DiagnosticLog}" FontFamily="Consolas" FontSize="14" TextWrapping="Wrap" LineHeight="20"/>
                        </Border>
                    </ScrollViewer>
                    
                    <Button Grid.Row="2" Content="إغلاق التقرير (Close)" 
                            Command="{Binding ToggleDiagnosticsCommand}"
                            Padding="30,12" Margin="0,20,0,0" HorizontalAlignment="Center"
                            Background="#34495E" Foreground="White" FontWeight="Bold">
                        <Button.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="8"/>
                            </Style>
                        </Button.Resources>
                    </Button>
                </Grid>
            </Border>
        </Border>
    </Grid>
</UserControl>
