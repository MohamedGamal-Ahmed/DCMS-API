<Application x:Class="DCMS.WPF.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:converters="clr-namespace:DCMS.WPF.Converters">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <materialDesign:BundledTheme BaseTheme="Light" PrimaryColor="DeepPurple" SecondaryColor="Lime" />
            </ResourceDictionary.MergedDictionaries>

            <!-- Converters -->
            <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
            <converters:BoolToVisibilityConverter x:Key="BoolToVisConverter"/>
            <converters:InverseBooleanToVisibilityConverter x:Key="InverseBooleanToVisibilityConverter"/>
            <converters:StringToVisibilityConverter x:Key="StringToVisibilityConverter"/>
            <converters:EqualityConverter x:Key="EqualityConverter"/>
            <converters:PercentageConverter x:Key="PercentageConverter"/>
            <converters:RandomWidthConverter x:Key="RandomWidthConverter"/>
            <converters:MultiValueConverter x:Key="MultiValueConverter"/>
            
            <!-- Chat Converters -->
            <local:BooleanToAlignmentConverter x:Key="BooleanToAlignmentConverter" xmlns:local="clr-namespace:DCMS.WPF.Views"/>
            <local:ConnectionStatusToColorConverter x:Key="ConnectionStatusToColorConverter" xmlns:local="clr-namespace:DCMS.WPF.Views"/>
            <local:ConnectionStatusToTextConverter x:Key="ConnectionStatusToTextConverter" xmlns:local="clr-namespace:DCMS.WPF.Views"/>
            <local:BooleanToChatColorConverter x:Key="BooleanToChatColorConverter" xmlns:local="clr-namespace:DCMS.WPF.Views"/>
            <local:BooleanToHeaderColorConverter x:Key="BooleanToHeaderColorConverter" xmlns:local="clr-namespace:DCMS.WPF.Views"/>
            <local:RoleToTextConverter x:Key="RoleToTextConverter" xmlns:local="clr-namespace:DCMS.WPF.Views"/>
            <local:InverseBooleanConverter x:Key="InverseBooleanConverter" xmlns:local="clr-namespace:DCMS.WPF.Views"/>
            <local:NullToBooleanConverter x:Key="NullToBooleanConverter" xmlns:local="clr-namespace:DCMS.WPF.Views"/>
            <local:UserInitialsConverter x:Key="UserInitialsConverter" xmlns:local="clr-namespace:DCMS.WPF.Views"/>
            <local:InitialsToColorConverter x:Key="InitialsToColorConverter" xmlns:local="clr-namespace:DCMS.WPF.Views"/>
            <local:IntToVisibilityConverter x:Key="IntToVisibilityConverter" xmlns:local="clr-namespace:DCMS.WPF.Views"/>
            
            <!-- Navigation Button Style (Legacy) -->
            <Style x:Key="NavButtonStyle" TargetType="Button">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="White"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Padding" Value="20,15"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="HorizontalContentAlignment" Value="Right"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Margin" Value="0,0,0,5"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}"
                                    BorderThickness="0"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                VerticalAlignment="Center"/>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
                <Style.Triggers>
                    <Trigger Property="IsMouseOver" Value="True">
                        <Setter Property="Background">
                            <Setter.Value>
                                <SolidColorBrush Color="#34495E" Opacity="0.8"/>
                            </Setter.Value>
                        </Setter>
                    </Trigger>
                </Style.Triggers>
            </Style>

            <!-- Navigation RadioButton Style -->
            <Style x:Key="NavRadioButtonStyle" TargetType="RadioButton">
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="Foreground" Value="#BDC3C7"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Padding" Value="20,15"/>
                <Setter Property="FontSize" Value="14"/>
                <Setter Property="HorizontalContentAlignment" Value="Right"/>
                <Setter Property="Cursor" Value="Hand"/>
                <Setter Property="Margin" Value="0,0,0,5"/>
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="RadioButton">
                            <Border x:Name="border" 
                                    Background="{TemplateBinding Background}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    Padding="{TemplateBinding Padding}">
                                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#34495E"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                                <Trigger Property="IsChecked" Value="True">
                                    <Setter TargetName="border" Property="Background" Value="#2980B9"/> <!-- Active Blue -->
                                    <Setter TargetName="border" Property="BorderThickness" Value="0,0,4,0"/>
                                    <Setter TargetName="border" Property="BorderBrush" Value="#3498DB"/>
                                    <Setter Property="Foreground" Value="White"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
