<UserControl x:Class="DCMS.WPF.Controls.ToastControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:services="clr-namespace:DCMS.WPF.Services"
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="300"
             Margin="0,5">
    <UserControl.Resources>
        <services:NotificationTypeToColorConverter x:Key="TypeToColorConverter"/>
        <services:NotificationTypeToIconConverter x:Key="TypeToIconConverter"/>
    </UserControl.Resources>

    <Border x:Name="ToastBorder" CornerRadius="8" Padding="15,10" BorderThickness="1">
        <Border.Style>
            <Style TargetType="Border">
                <Setter Property="Background" Value="White"/>
                <Setter Property="BorderBrush" Value="{Binding Type, Converter={StaticResource TypeToColorConverter}}"/>
                <Style.Triggers>
                    <EventTrigger RoutedEvent="Loaded">
                        <BeginStoryboard>
                            <Storyboard>
                                <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.3"/>
                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)" From="100" To="0" Duration="0:0:0.4">
                                    <DoubleAnimation.EasingFunction>
                                        <CubicEase EasingMode="EaseOut"/>
                                    </DoubleAnimation.EasingFunction>
                                </DoubleAnimation>
                            </Storyboard>
                        </BeginStoryboard>
                    </EventTrigger>
                </Style.Triggers>
            </Style>
        </Border.Style>
        <Border.RenderTransform>
            <TranslateTransform/>
        </Border.RenderTransform>
        <Border.Effect>
            <DropShadowEffect Color="#CCCCCC" BlurRadius="10" ShadowDepth="2" Opacity="0.4"/>
        </Border.Effect>

        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="{Binding Type, Converter={StaticResource TypeToIconConverter}}" 
                       FontSize="20" VerticalAlignment="Center" Margin="0,0,10,0"/>
            
            <TextBlock Grid.Column="1" Text="{Binding Message}" 
                       VerticalAlignment="Center" FontSize="14" FontWeight="SemiBold" 
                       Foreground="#333" TextWrapping="Wrap"/>
        </Grid>
    </Border>
</UserControl>
